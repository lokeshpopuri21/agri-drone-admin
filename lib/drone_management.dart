import 'package:flutter/material.dart';

class DroneManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Drone Management'),
      ),
      body: Center(
        child: Text('Welcome, Admin!'),
      ),
    );
  }
}
